# YWAgentPlatform 部署文档

## 环境信息

| 项目 | 说明 |
|------|------|
| 服务器 | 10.225.138.183 |
| 部署路径 | /data_ca/YWAgentPlatform |
| Python | 3.12 |
| Node.js | 20.x |
| Docker | 26.x (含 Compose 插件) |

## 架构说明

```
数据库层 (docker-compose.db.yml):
  MySQL + Redis → 独立管理，长期运行

应用层 (docker-compose.yml):
  Backend + Frontend + Nginx → 代码更新时重建

开发模式:
  本地 PC (前端dev + 后端dev) → 10.225.138.183 (MySQL + Redis)

正式部署:
  10.225.138.183 (应用容器) → 10.225.138.183 (数据库容器)
```

---

## 一、数据库部署 (首次)

> 数据库只部署一次，后续开发和正式环境共用。

### 1.1 服务器准备

```bash
ssh root@10.225.138.183

# 确认 Docker 和 Compose 插件
docker --version
docker compose version
```

### 1.2 克隆项目

```bash
cd /data_ca
git clone https://github.com/Chan0615/YWAgentPlatform.git YWAgentPlatform
cd /data_ca/YWAgentPlatform
```

### 1.3 创建根目录 .env

```bash
cp .env.example .env
vi .env
```

填入 MySQL 密码：
```env
MYSQL_ROOT_PASSWORD=<你的MySQL密码>
MYSQL_DATABASE=yw_agent_portal
REDIS_PASSWORD=<你的Redis密码>
```

### 1.4 启动数据库

```bash
docker compose -f docker-compose.db.yml up -d
```

### 1.5 等待初始化完成

```bash
# 等待 30 秒让 MySQL 执行 init.sql
sleep 30

# 验证容器状态
docker ps | grep YWAgentPlatform

# 验证数据库表已创建
docker exec -it YWAgentPlatform-MySQL mysql -uroot -p"<你的MySQL密码>" -e "
  USE yw_agent_portal;
  SHOW TABLES;
  SELECT username, nickname FROM users;
"

# 验证 Redis
docker exec -it YWAgentPlatform-Redis redis-cli -a "<你的Redis密码>" ping
```

应看到 7 张表和 1 个 admin 用户。

### 1.6 数据库初始数据说明

初始数据由 `docker/mysql/init.sql` 自动执行，包含：

| 内容 | 数量 |
|------|------|
| 默认用户 | 2 (admin / readonly) |
| 角色 | 2 (超级管理员 / 只读用户) |
| 权限 | 22 条 (app/menu/button 三级) |
| 注册应用 | 0（默认不预置，后续在“应用管理”页面手动新增） |

平台默认账号:

- `admin` / `admin123`
- `readonly` / `readonly123`

---

## 二、开发环境部署 (本地 Windows)

### 2.1 前提条件

- 服务器数据库已启动 (步骤一)
- 本地安装 Python 3.12
- 本地安装 Node.js 20

### 2.2 创建后端配置

```bash
cd D:\00_ChAn_Workspace\YWAgentPlatform\backend
copy .env.example .env
```

编辑 `backend/.env`，修改以下字段：
```env
DB_HOST=10.225.138.183
DB_PORT=3306
DB_USER=root
DB_PASSWORD=<你的MySQL密码>
DB_NAME=yw_agent_portal

REDIS_HOST=10.225.138.183
REDIS_PORT=6379
REDIS_PASSWORD=<你的Redis密码>

JWT_SECRET_KEY=<自定义一个JWT密钥>

DEBUG=true
```

### 2.3 启动后端

```bash
cd D:\00_ChAn_Workspace\YWAgentPlatform\backend

# 创建虚拟环境
py -3.12 -m venv .venv

# 激活
.venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 启动
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

验证: 访问 http://localhost:8000/docs 看到 Swagger 文档即成功。

### 2.4 启动前端

```bash
cd D:\00_ChAn_Workspace\YWAgentPlatform\frontend

# 安装依赖
npm install --registry=https://registry.npmmirror.com

# 启动
npm run dev
```

验证: 访问 http://localhost:5173 看到登录页面即成功。

### 2.5 开发环境数据流

```
浏览器 (localhost:5173)
  → Vite Dev Server
  → /api 代理到 localhost:8000
  → 10.225.138.183:3306 (MySQL)
  → 10.225.138.183:6379 (Redis)
```

---

## 三、正式环境部署

### 3.1 前提条件

- 服务器数据库已启动 (步骤一)
- 代码已推送到 git 仓库

### 3.2 创建后端配置

```bash
cd /data_ca/YWAgentPlatform/backend
cp .env.example .env
vi .env
```

填入实际值：
```env
DB_HOST=10.225.138.183
DB_PORT=3306
DB_USER=root
DB_PASSWORD=<你的MySQL密码>
DB_NAME=yw_agent_portal

REDIS_HOST=10.225.138.183
REDIS_PORT=6379
REDIS_PASSWORD=<你的Redis密码>

JWT_SECRET_KEY=<生产环境JWT密钥>

CORS_ORIGINS=["http://10.225.138.183","http://yw.ops.com","https://yw.ops.com"]

DEBUG=false
```

> Docker 部署时 `docker-compose.yml` 会自动覆盖 `DB_HOST` 和 `REDIS_HOST` 为容器可达地址。

### 3.3 一键部署

```bash
cd /data_ca/YWAgentPlatform
chmod +x deploy.sh
./deploy.sh init
```

或手动分步：
```bash
# 构建并启动应用
docker compose up -d --build

# 查看状态
docker compose ps
```

### 3.4 期望容器状态

```
NAME                       STATUS    PORTS
YWAgentPlatform-MySQL      Up        0.0.0.0:3306->3306/tcp
YWAgentPlatform-Redis      Up        0.0.0.0:6379->6379/tcp
YWAgentPlatform-Backend    Up        0.0.0.0:8010->8000/tcp
YWAgentPlatform-Frontend   Up        80/tcp
YWAgentPlatform-Nginx      Up        0.0.0.0:80->80/tcp
```

### 3.5 访问

| 地址 | 说明 |
|------|------|
| http://10.225.138.183 | 门户首页 |
| http://10.225.138.183:8010/docs | API 文档 |

### 3.6 配置域名 (可选)

修改本地 hosts：
```
10.225.138.183  yw.ops.com
```

或在公司 DNS 添加 A 记录: `yw.ops.com → 10.225.138.183`

---

## 四、日常更新

### 4.1 使用 deploy.sh

```bash
cd /data_ca/YWAgentPlatform

./deploy.sh              # 更新全部 (git pull + 重建应用)
./deploy.sh backend      # 仅更新后端
./deploy.sh frontend     # 仅更新前端
./deploy.sh db           # 重启数据库
./deploy.sh status       # 查看状态
./deploy.sh logs         # 查看日志
./deploy.sh logs backend # 查看后端日志
./deploy.sh restart      # 重启所有应用
./deploy.sh down         # 停止应用 (不影响数据库)
```

### 4.2 手动更新

```bash
cd /data_ca/YWAgentPlatform
git pull

# 仅更新后端
docker compose up -d --build backend

# 仅更新前端
docker compose up -d --build frontend
docker compose restart nginx
```

---

## 五、子应用接入说明

平台中的“应用管理”负责维护的是子应用入口，不负责存放子应用代码。

### 5.1 子应用代码放哪里

建议在服务器上单独放置子应用项目，例如：

```text
/data_ca/
  YWAgentPlatform/     # 门户平台
  AgenticOps/          # 智能运维
  agent_platform/      # Agent 平台
  opsflow/             # 新工单流程平台
```

每个子应用独立部署、独立端口运行。

### 5.2 新增一个子应用的标准流程

以 `opsflow` 为例：

1. 将代码部署到服务器目录，例如 `/data_ca/opsflow`
2. 启动子应用，例如监听端口 `8088`
3. 在 `YWAgentPlatform/nginx/default.conf` 增加反向代理：

```nginx
location /app/opsflow/ {
    proxy_pass http://10.225.138.183:8088/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

4. 重启 YWAgentPlatform 的 Nginx：

```bash
cd /data_ca/YWAgentPlatform
docker compose restart nginx
```

5. 在门户“系统管理 -> 应用管理”中新增应用：

```text
应用名称: 工单流程
应用编码: opsflow
访问地址: /app/opsflow/
图标: ProfileOutlined
```

### 5.3 访问地址怎么填

- 推荐填写相对路径：`/app/应用编码/`
- 例如：`/app/opsflow/`
- 不推荐直接填 `http://IP:端口/`，否则可能有跨域和登录态传递问题

### 5.4 图标怎么选

当前应用管理页支持下拉选择平台内置图标，保持风格统一。

如果以后要支持“用户上传自定义图标”，需要额外补：
- 上传接口
- 图标文件存储目录
- 预览和回显逻辑

目前建议先使用平台内置图标。

---

## 六、数据库运维

### 6.1 连接数据库

```bash
docker exec -it YWAgentPlatform-MySQL mysql -uroot -p"<密码>" yw_agent_portal
```

### 6.2 备份

```bash
docker exec YWAgentPlatform-MySQL mysqldump -uroot -p"<密码>" yw_agent_portal > backup_$(date +%Y%m%d_%H%M%S).sql
```

### 6.3 恢复

```bash
docker exec -i YWAgentPlatform-MySQL mysql -uroot -p"<密码>" yw_agent_portal < backup_file.sql
```

### 6.4 重置数据库 (危险)

```bash
# 停止并删除数据卷，重新初始化
docker compose -f docker-compose.db.yml down -v
docker compose -f docker-compose.db.yml up -d
# 等待 30 秒
```

---

## 六、配置文件说明

### 需手动创建的文件 (不提交 git)

| 文件 | 用途 | 创建方式 |
|------|------|----------|
| `.env` | docker-compose 读取 MySQL 密码 | `cp .env.example .env` |
| `backend/.env` | 后端配置 (密码/密钥/连接信息) | `cp backend/.env.example backend/.env` |

### git 中的模板文件

| 文件 | 说明 |
|------|------|
| `.env.example` | 根目录 env 模板 |
| `backend/.env.example` | 后端 env 模板 |

### Docker Compose 文件

| 文件 | 管理内容 | 说明 |
|------|----------|------|
| `docker-compose.db.yml` | MySQL + Redis | 独立管理，长期运行 |
| `docker-compose.yml` | Backend + Frontend + Nginx | 应用层，更新代码时重建 |

### 为什么只有一个 backend/.env

`docker-compose.yml` 中通过 `environment` 字段覆盖了 `DB_HOST` 和 `REDIS_HOST`：
- 本地开发 → 直接读 `.env` 中的 IP 地址
- Docker 部署 → 被 `docker-compose.yml` 中的 environment 覆盖为宿主机 IP

一份配置文件，两种场景自动适配。

---

## 七、防火墙

```bash
# 确保开放端口
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=443/tcp
firewall-cmd --permanent --add-port=3306/tcp   # MySQL (开发需要，生产建议限制IP)
firewall-cmd --permanent --add-port=6379/tcp   # Redis (开发需要，生产建议限制IP)
firewall-cmd --permanent --add-port=8010/tcp   # API (调试用)
firewall-cmd --reload
```

---

## 八、故障排查

### 后端连不上数据库

```bash
# 1. 确认 MySQL 容器在运行
docker ps | grep YWAgentPlatform-MySQL

# 2. 确认端口监听
ss -tlnp | grep 3306

# 3. 本地测试连接
python -c "
import pymysql
conn = pymysql.connect(host='10.225.138.183', port=3306, user='root', password='<密码>', db='yw_agent_portal')
print('连接成功')
conn.close()
"
```

### CORS 错误

检查 `backend/.env` 中 `CORS_ORIGINS` 是否包含前端访问地址。

### Docker 构建失败

```bash
docker compose build --no-cache
docker compose build backend 2>&1 | tail -50
```

### init.sql 没执行

MySQL 容器只在首次创建数据卷时执行 init.sql：
```bash
docker compose -f docker-compose.db.yml down -v
docker compose -f docker-compose.db.yml up -d
```
